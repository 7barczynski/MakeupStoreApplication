<thlogic>
    <!-- Head -->
<!--    <attr sel="#headTemplate"-->
<!--          th:replace="~{fragments/head :: headFragment}"/>-->
    <attr sel="#appName"
          th:text="${@AppProperties.getApplicationName()}"/>

    <!-- Top header -->
    <attr sel="#topHeader"
          th:replace="~{fragments/top-header :: topHeaderFragment}"/>

    <!-- Menu -->
    <attr sel="#mainMenu"
          th:replace="~{fragments/main-menu :: mainMenuFragment}"/>

    <!-- Products categories -->
    <attr sel="#productCategoriesHead"
          th:text="#{product.categories.name}"/>
    <attr sel="#viewProductsName"
          th:text="#{view.products.name}"/>

    <attr sel="#eyesCategoryLink"
          th:href="@{__${T(com.tbar.MakeupStoreApplication.utility.AppMappings).SHOP}__}"/>
    <attr sel="#eyesCategoryImage"
          th:src="@{__${@AppProperties.getEyesImageUrl()}__}"/>
    <attr sel="#eyesName"
          th:text="#{eyes.name}"/>

    <attr sel="#lipsCategoryImage"
          th:src="@{__${@AppProperties.getLipsImageUrl()}__}"/>
    <attr sel="#lipsCategoryLink"
          th:href="@{__${T(com.tbar.MakeupStoreApplication.utility.AppMappings).SHOP}__}"/>
    <attr sel="#lipsName"
          th:text="#{lips.name}"/>

    <attr sel="#faceCategoryImage"
          th:src="@{__${@AppProperties.getFaceImageUrl()}__}"/>
    <attr sel="#faceCategoryLink"
          th:href="@{__${T(com.tbar.MakeupStoreApplication.utility.AppMappings).SHOP}__}"/>
    <attr sel="#faceName"
          th:text="#{face.name}"/>

    <!-- Trendy Products -->
    <attr sel="#trendyProducts"
          th:text="#{trendy.products}"/>

    <!-- Scripts -->
    <attr sel="#languageVariableInit"
          th:inline="javascript"
          th:utext='|var currentLanguage = "${__${T(com.tbar.MakeupStoreApplication.utility.AttributeNames).CURRENT_LANGUAGE}__}";
                    var defaultLanguage = "${__${@AppProperties.getApplicationDefaultLanguage()}__}";|'/>
</thlogic>
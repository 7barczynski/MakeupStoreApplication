<thlogic>
    <attr sel="#breadcrumbsFragment"
          th:fragment="breadcrumbsFragment"/>
    <attr sel="#pageName"
          th:text="${pageName}"/>
    <attr sel="#homeLink"
          th:text="${homeName}"
          th:href="@{__${T(com.tbar.makeupstoreapplication.utility.AppMappings).HOME}__}"/>
</thlogic>
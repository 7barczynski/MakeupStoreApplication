<thlogic>
    <!-- Head -->
    <attr sel="#headTemplate"
        th:replace="~{fragments/head :: headFragment}"/>

    <!-- Top header -->
    <attr sel="#topHeaderTemplate"
        th:replace="~{fragments/top-header :: topHeaderFragment}"/>

    <!-- Menu -->
    <attr sel="#mainMenuTemplate"
        th:replace="~{fragments/main-menu :: mainMenuFragment}"/>

    <!-- Breadcrumbs -->
<!--    TODO Breadcrumby są bardzo źle zrobione, może da się łatwo wrzucić listę z przetłumaczonymi nazwami oraz z linkami?-->
    <attr sel="#breadcrumbsTemplate"
          th:replace="~{fragments/breadcrumbs :: breadcrumbsFragment(pageName=#{shop.name}, homeName=#{home.name})}"/>

    <!-- Filter -->
    <attr sel="#filter"
          th:attr="action=@{__${T(com.tbar.MakeupStoreApplication.utility.AppMappings).SHOP}__}"/>
    <attr sel="#typeName"
          th:text="#{type.name}"/>
    <attr sel="#typeForEach"
          th:each="type : ${@AppProperties.getProductTypeList()}"
          th:text="#{__${type}__.name}"
          th:attr="value=${type}"/>
    <attr sel="#categoryName"
          th:text="#{category.name}"/>
    <attr sel="#categoryForEach"
          th:each="category : ${@AppProperties.getProductCategoryList()}"
          th:text="#{__${category}__.name}"
          th:attr="value=${category}"/>
    <attr sel="#brandName"
          th:text="#{brand.name}"/>
    <attr sel="#brandForEach"
          th:each="brand : ${@AppProperties.getBrandList()}"
          th:text="${#strings.capitalize(brand)}"
          th:attr="value=${brand}"/>
    <attr sel="#tagsName"
          th:text="#{tags.name}"/>
    <attr sel="#tagsForEach"
          th:each="tag : ${@AppProperties.getProductTagsList()}"
          th:text="#{__${tag}__.name}"
          th:attr="value=${tag}"/>
    <attr sel="#priceName"
          th:text="#{price.name}"/>
    <attr sel="#ratingName"
          th:text="#{rating.name}"/>
    <attr sel="#filterName"
          th:text="#{filter.name}"/>

    <!-- Product List -->
    <attr sel="#itemListSwitch"
          th:switch="${__${T(com.tbar.MakeupStoreApplication.utility.AttributeNames).ITEMS_PAGE_LIST}__}"/>
    <attr sel="#itemsNullCase"
          th:case="null"/>
    <attr sel="#notFoundId"
          th:text="#{product.not.found}"/>
    <attr sel="#itemsDefaultCase"
          th:case="*"/>
    <attr sel="#itemsForEach"
          th:each="item : ${__${T(com.tbar.MakeupStoreApplication.utility.AttributeNames).ITEMS_PAGE_LIST}__}"/>
    <attr sel="#itemTemplate"
          th:replace="~{fragments/item :: itemFragment(item=${item})}"/>

    <!-- Pagination -->
    <attr sel="#paginationCondition"
          th:if="${__${T(com.tbar.MakeupStoreApplication.utility.AttributeNames).ITEMS_PAGE_LIST}__ != null and
            __${T(com.tbar.MakeupStoreApplication.utility.AttributeNames).ITEMS_PAGE_LIST}__.totalPages > 1}"
          th:with="page=${__${T(com.tbar.MakeupStoreApplication.utility.AttributeNames).ITEMS_PAGE_LIST}__},
                    shopMapping=${T(com.tbar.MakeupStoreApplication.utility.AppMappings).SHOP},
                    queryParamPage=${T(com.tbar.MakeupStoreApplication.utility.AppMappings).QUERY_PARAM_PAGE},
                    urlBuilder=${T(org.springframework.web.servlet.support.ServletUriComponentsBuilder).fromCurrentRequest()}"/>
    <attr sel="#previousIconCondition"
          th:if="${page.hasPrevious() && page.hasContent()}"/>
    <attr sel="#previousLink"
          th:href="${urlBuilder.replaceQueryParam('__${queryParamPage}__', '__${page.number}__').toUriString()}"/>
    <attr sel="#paginationList"
          th:each="pageNumber : ${__${T(com.tbar.MakeupStoreApplication.utility.AttributeNames).PAGINATION_NUMBERS_LIST}__}"
          th:href="${urlBuilder.replaceQueryParam('__${queryParamPage}__', '__${pageNumber}__').toUriString()}"
          th:text="${pageNumber}"
          th:class="${pageNumber==page.number + 1} ? active"/>
    <attr sel="#nextIconCondition"
          th:if="${page.hasNext() && page.hasContent()}"/>
    <attr sel="#nextLink"
          th:href="${urlBuilder.replaceQueryParam('__${queryParamPage}__', '__${page.number+2}__').toUriString()}"/>


    <!-- Scripts -->
    <attr sel="#scriptTemplate"
        th:replace="~{fragments/script :: scriptFragment}"/>

</thlogic>